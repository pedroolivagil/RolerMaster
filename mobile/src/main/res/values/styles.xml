<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="FragmentTitle" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textSize">@dimen/fragment_title_text</item>
        <item name="android:layout_marginBottom">@dimen/fragment_title_margin_top_btm</item>
        <item name="android:layout_marginTop">@dimen/fragment_title_margin_top_btm</item>
        <item name="android:layout_marginLeft">@dimen/fragment_title_margin_lft_rgt</item>
        <item name="android:layout_marginRight">@dimen/fragment_title_margin_lft_rgt</item>
        <item name="android:textColor">@color/fragment_title</item>
        <item name="android:shadowColor">@color/colorAccent</item>
        <item name="android:shadowDx">-2</item>
        <item name="android:shadowDy">2</item>
        <item name="android:shadowRadius">10</item>
    </style>

</resources>
